{% extends 'base.html.twig' %}

{% block title %}Add post{% endblock %}

{% block body %}
<div class="container" style="margin-top: 22vh; ">
        <h1>Ajouter une nouvelle article</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Sauvgarder l'article</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

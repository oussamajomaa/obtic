{% extends 'base.html.twig' %}

{% block title %}Add Category{% endblock %}

{% block body %}
<div class="container">
        <h1>Ajouter une nouvelle catégorie</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Sauvgarder la catégorie</button>
        {{ form_end(form) }}
    </div>
{% endblock %}